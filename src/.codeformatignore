# Files and directories under src/ excluded from EmmyLuaCodeStyle formatting.
#
# One glob per line, '#' starts a comment. The list must stay as short as
# possible: prefer fixing the code or tuning .editorconfig over adding an
# entry here. Every entry needs an explanation.

# Vendored copy of tarantool/conf + etcd-client. It is a separate nested git
# repository (src/lua/conf/.git -> .git/modules/conf) and is not tracked by the
# main Tarantool repository, so formatting changes could not be committed.
src/lua/conf/**

# Generated by gen-zone-abbrevs.pl via the `gen-timezones` CMake target.
# Re-running the build overwrites the file, so formatting would be lost.
src/lua/timezones.lua
